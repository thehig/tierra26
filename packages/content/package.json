{
  "name": "@tierra26/content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "tierra26 learning content — lesson schema, playground contract, per-instruction data, keyword registry, progression, and goal-checking (data + logic; rendering lives in the UI layer)",
  "scripts": {
    "test": "node --experimental-strip-types --test \"test/**/*.test.ts\"",
    "test:watch": "node --experimental-strip-types --test --watch \"test/**/*.test.ts\"",
    "spec": "node --experimental-strip-types --test-reporter=spec --test \"test/**/*.test.ts\""
  }
}
