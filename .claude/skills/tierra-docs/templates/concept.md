---
slug: «slug»
title: «slug» («a short lower-case noun phrase, no final period»)
emoji: «glyph»
category: «action | register | marker | control | value | concept»
---

# «slug»

## Simple
«What it is, in one sentence a ten-year-old finishes without stopping.» «Two or three
more that build the picture — physical and concrete: boxes, rooms, signposts, a head
that walks.» «Chip the page's own subject here: "A {«slug»} is …", and tag every other
named part of the machine you mention — `npm run docs:vocab` for what resolves.»

## Advanced
«The precise model. Name the constants that matter (`MAX_TEMPLATE = 10`,
`STACK_SIZE = 10`, `movThrScaled` defaults to 700 per-1000) and the engine identifiers a
reader would grep for.»

«Structure a long Advanced section with bold lead-ins — **Measurement.**, **Complement
match.**, **Search & direction.**, **Merge rule.** — one paragraph each.»

## Edge Cases
«Optional. Add it when the concept has real boundaries — `gates`, `mutation`, `reaper`
and `save-pile` all earn one. Delete the heading if it does not.»

- «The zero case, the wrap case, the empty case, the exactly-at-the-boundary case.»
- «What each boundary makes a reader wrongly conclude.»

«Prefer showing a boundary to describing it: set the `## Try it` stage up so it lands
on the edge case, rather than starting from a blank creature.»

## Try it
«Optional. A stage earns its place when the idea is a change over time or a position in
space — a head moving, a daughter filling, a search walking outward. Delete if the idea
is a fact rather than a motion.»

<EntityDesigner soup="36">
  <Genome>
«the smallest genome that makes the idea visible»
  </Genome>
</EntityDesigner>

«One sentence naming what to watch.»

## See also
- [«concept-slug»](«concept-slug».md), [«concept-slug»](«concept-slug».md)
- [«mnemonic»](../opcodes/«mnemonic».md), [«mnemonic»](../opcodes/«mnemonic».md)
